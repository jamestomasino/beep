--  Root namespace package for beep.
package Beep is

   --  Single source of truth for the version string.
   --  Keep in sync with the "version" field in alire.toml.
   --  Bump both together on a release.
   Version : constant String := "1.0.0";

end Beep;
